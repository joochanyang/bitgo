# Secrets & runtime state — NEVER commit (API keys / live balances / trade history)
config.json
.env
.env.local
logs.json
trades.json
agent_memory.json

# Build artifacts
/gobot
/gobot-*
/agent
*.exe
*.out

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
